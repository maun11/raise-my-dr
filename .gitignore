# Internal ops notes — sheet IDs, n8n instance, credential names. Never publish.
HANDOVER.md

.claude/settings.local.json
__pycache__/
*.pyc
.DS_Store

# Built bundle — distributed via GitHub Releases, not committed
dist/

# Launch copy — local working files, not part of the distributed skill
community/
