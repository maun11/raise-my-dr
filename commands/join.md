---
description: Check whether your site clears the gate, then apply to the exchange
argument-hint: "[your domain]"
---

Run **Flow C — Join** from the Backlink Exchange skill (`SKILL.md` at the plugin
root).

Domain, if supplied: $ARGUMENTS

Assess the site honestly against `references/vetting.md` before pointing them at
the application. Never promise membership — the gate is automated and a human
reviews every row.
